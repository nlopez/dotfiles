.venv

# graphify -- local metadata, not shared
graphify-out/cost.json
# Semantic cache — duplicates graph.json data, grows fast
graphify-out/cache/
# Python interpreter path — machine-specific
graphify-out/.graphify_python
# Root path — machine-specific
graphify-out/.graphify_root

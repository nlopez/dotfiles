#!/bin/bash
# Rebuild font cache after font-microsoft-office cask is updated.
# run_onchange: triggered when the cask content/checksum changes.

set -eufo pipefail

if command -v fc-cache &>/dev/null; then
  fc-cache -fv ~/.local/share/fonts 2>&1 || true
fi

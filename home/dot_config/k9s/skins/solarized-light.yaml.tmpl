{{- $s := .solarized -}}
# Solarized Light skin — https://ethanschoonover.com/solarized/
k9s:
  body:
    fgColor: "{{ $s.base00 }}"
    bgColor: "{{ $s.base3 }}"
    logoColor: "{{ $s.blue }}"
  prompt:
    fgColor: "{{ $s.base00 }}"
    bgColor: "{{ $s.base3 }}"
    suggestColor: "{{ $s.orange }}"
  info:
    fgColor: "{{ $s.magenta }}"
    sectionColor: "{{ $s.base00 }}"
  dialog:
    fgColor: "{{ $s.base00 }}"
    bgColor: "{{ $s.base3 }}"
    buttonFgColor: "{{ $s.base00 }}"
    buttonBgColor: "{{ $s.magenta }}"
    buttonFocusFgColor: white
    buttonFocusBgColor: "{{ $s.cyan }}"
    labelFgColor: "{{ $s.orange }}"
    fieldFgColor: "{{ $s.base00 }}"
  frame:
    border:
      fgColor: "{{ $s.base2 }}"
      focusColor: "{{ $s.base00 }}"
    menu:
      fgColor: "{{ $s.base00 }}"
      keyColor: "{{ $s.magenta }}"
      numKeyColor: "{{ $s.magenta }}"
    crumbs:
      fgColor: white
      bgColor: "{{ $s.cyan }}"
      activeColor: "{{ $s.yellow }}"
    status:
      newColor: "{{ $s.cyan }}"
      modifyColor: "{{ $s.blue }}"
      addColor: "{{ $s.green }}"
      errorColor: "{{ $s.red }}"
      highlightColor: "{{ $s.orange }}"
      killColor: "{{ $s.base1 }}"
      completedColor: "{{ $s.base1 }}"
    title:
      fgColor: "{{ $s.base00 }}"
      bgColor: "{{ $s.base3 }}"
      highlightColor: "{{ $s.blue }}"
      counterColor: "{{ $s.magenta }}"
      filterColor: "{{ $s.magenta }}"
  views:
    charts:
      bgColor: default
      defaultDialColors:
        - "{{ $s.blue }}"
        - "{{ $s.red }}"
      defaultChartColors:
        - "{{ $s.blue }}"
        - "{{ $s.red }}"
    table:
      fgColor: "{{ $s.base00 }}"
      bgColor: "{{ $s.base3 }}"
      cursorFgColor: white
      cursorBgColor: "{{ $s.base3 }}"
      markColor: darkgoldenrod
      header:
        fgColor: "{{ $s.base00 }}"
        bgColor: "{{ $s.base3 }}"
        sorterColor: "{{ $s.cyan }}"
    xray:
      fgColor: "{{ $s.base00 }}"
      bgColor: "{{ $s.base3 }}"
      cursorColor: "{{ $s.base2 }}"
      graphicColor: "{{ $s.blue }}"
      showIcons: false
    yaml:
      keyColor: "{{ $s.magenta }}"
      colonColor: "{{ $s.blue }}"
      valueColor: "{{ $s.base00 }}"
    logs:
      fgColor: "{{ $s.base00 }}"
      bgColor: "{{ $s.base3 }}"
      indicator:
        fgColor: "{{ $s.base00 }}"
        bgColor: "{{ $s.base2 }}"
        toggleOnColor: "{{ $s.magenta }}"
        toggleOffColor: "{{ $s.blue }}"

{{- if not .headless -}}
# 1Password SSH agent config — pin the exact items the agent offers.
# Docs: https://developer.1password.com/docs/ssh/agent/config

[[ssh-keys]]
item = "GitHub - nlopez"
vault = "Private"
{{ if .work }}
[[ssh-keys]]
# Rename your work SSH key item in 1Password to "GitHub - work" to match.
item = "GitHub - work"
vault = "Employee"
{{- else if .personal }}
[[ssh-keys]]
item = "GitHub - SilentShout42"
vault = "Private"
{{- end }}
{{- end }}
